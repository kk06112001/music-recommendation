Content-based song recommendations model by combining text data (via TF-IDF vectorization) and numeric features (scaled for consistency) to compute similarities and suggest songs based on the input title
